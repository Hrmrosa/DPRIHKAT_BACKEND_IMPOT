package com.DPRIHKAT.config;

import org.springframework.context.annotation.Configuration;

/**
 * Configuration CORS - Désactivée car gérée dans SecurityConfig
 * Pour éviter les conflits, la configuration CORS est centralisée dans SecurityConfig.java
 */
@Configuration
public class CorsConfig {
    // Configuration CORS déplacée dans SecurityConfig pour éviter les conflits
}
