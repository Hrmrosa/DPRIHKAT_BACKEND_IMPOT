package com.DPRIHKAT.entity.enums;

/**
 * Formats disponibles pour les rapports programmés
 */
public enum FormatRapport {
    PDF,        // Format PDF
    EXCEL,      // Format Excel
    CSV,        // Format CSV
    HTML,       // Format HTML (dans le corps de l'email)
    JSON        // Format JSON
}
